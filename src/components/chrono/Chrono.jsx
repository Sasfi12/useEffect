export default function Chrono({minutes , seconds}) {
    return (
        <div className="count-exercice">
            <h1>Timecount Exercice</h1>
            <h2>Current Time : {minutes}: {seconds}</h2>
        </div>
    )
} 